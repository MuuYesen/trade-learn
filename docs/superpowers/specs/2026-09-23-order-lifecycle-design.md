# PR #4 order lifecycle repair

Approved direction: retain the contributor's PR ancestry, correct implementation to the existing architecture, preserve local work, verify before merge/tag publication.

Python normalizes user-facing order metadata at submission (UTC expiry, trailing parameters, explicit OCO links). Rust is authoritative for eligibility, matching, OCO exclusion, and portfolio updates. All Rust runners consume the same submission/control bridge, including callbacks returning no new orders. Python fallback uses the same native controls. Expiry is enforced before matching; Python mirrors terminal states from native events. Cancellation of not-yet-submitted orders removes their buffered submission and must not cancel a different native ID. Explicit OCO groups work in every matching mode; the existing smart-mode protective-exit policy is retained for compatibility. Trailing watermark advances only after an unfilled bar, monotonically from initial reference. Triggered trailing limit orders retain limit-only state across bars. Engine/Lite APIs remain compatible; preserve existing order constants rather than importing Backtrader numeric conventions. Internal richer backtest orders do not force changes to the broker-neutral core protocol.

Existing local strategy/metrics/data work must remain intact. Integration includes PR ancestry and focused corrections, with any local overlap merged explicitly. Release requires focused lifecycle matrix and existing relevant regressions, native wheel build and version consistency. Do not publish secrets or operational snapshots among unrelated local files.
